{
  "name": "chaser-v2",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time DMX sequencer with WebSocket architecture and retro web simulator",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "tsc --noEmit -p tsconfig.json",
    "artnet:test": "tsx src/tools/artnet-test.ts",
    "artnet:probe": "tsx src/tools/artnet-test.ts --probe"
  },
  "dependencies": {
    "@fastify/cors": "^10.1.0",
    "@fastify/static": "^8.1.0",
    "@fastify/websocket": "^11.0.0",
    "fastify": "^5.2.1",
    "mqtt": "^5.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
